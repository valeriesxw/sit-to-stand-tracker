apiVersion: v1
kind: Service
metadata:
  name: sit-to-stand
  namespace: sit-to-stand
spec:
  type: LoadBalancer
  selector:
    app: sit-to-stand
  ports:
    - name: http
      port: 80
      targetPort: http
